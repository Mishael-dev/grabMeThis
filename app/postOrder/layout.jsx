export const metadata = {
  title: "Place order",
  description: "Place Your order on this page",
};

export default function Layout({ children }) {
  return (
    <html lang="en">
      <body className={""}>{children}</body>
    </html>
  );
}
